import { loginValidator } from './login.validator';
import { registerValidator } from './register.validator';
import { verifyEmailValidator } from './email_verify.validator';

export { loginValidator, registerValidator, verifyEmailValidator };
