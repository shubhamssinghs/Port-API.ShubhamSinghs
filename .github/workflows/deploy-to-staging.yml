# name: CD - Deploy to Staging

# on:
#   branches:
#     - develop

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Build Docker image
#         run: |
#           docker build -t my-node-app .

#       - name: Log in to DockerHub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Push Docker image
#         run: |
#           docker tag my-node-app ${{ secrets.DOCKER_USERNAME }}/my-node-app:staging
#           docker push ${{ secrets.DOCKER_USERNAME }}/my-node-app:staging

#       - name: Deploy to staging server
#         env:
#           STAGING_SERVER_IP: ${{ secrets.STAGING_SERVER_IP }}
#           STAGING_SSH_KEY: ${{ secrets.STAGING_SSH_KEY }}
#         run: |
#           echo "$STAGING_SSH_KEY" > staging_private_key.pem
#           chmod 600 staging_private_key.pem
#           ssh -o StrictHostKeyChecking=no -i staging_private_key.pem user@$STAGING_SERVER_IP << 'EOF'
#             docker pull ${{ secrets.DOCKER_USERNAME }}/my-node-app:staging
#             docker stop my-node-app-staging || true
#             docker rm my-node-app-staging || true
#             docker run -d --name my-node-app-staging -p 8080:80 ${{ secrets.DOCKER_USERNAME }}/my-node-app:staging
#           EOF
