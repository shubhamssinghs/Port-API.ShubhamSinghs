name: Deploy to Dev

on:
  workflow_run:
    workflows:
      - Node Tests & SonarQube Scan
    types:
      - completed
  pull_request:
    branches:
      - develop

jobs:
  deploy_to_dev:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker image for Development
        run: |
          docker build -f Dockerfile.dev -t ${{ secrets.DOCKER_USERNAME }}/Port-API-ShubhamSinghs-Dev:latest .

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker image
        run: |
          docker tag /Port-API-ShubhamSinghs-Dev ${{ secrets.DOCKER_USERNAME }}/Port-API-ShubhamSinghs-Dev:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/Port-API-ShubhamSinghs-Dev:latest
