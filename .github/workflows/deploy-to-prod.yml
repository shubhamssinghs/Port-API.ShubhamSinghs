# name: CD - Deploy to Production

# on:
#   workflow_run:
#     workflows: ['CI - Node Tests']
#     types:
#       - completed

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Build Docker image
#         run: |
#           docker build -t my-node-app .

#       - name: Log in to DockerHub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Push Docker image
#         run: |
#           docker tag my-node-app ${{ secrets.DOCKER_USERNAME }}/my-node-app:latest
#           docker push ${{ secrets.DOCKER_USERNAME }}/my-node-app:latest

#       - name: Deploy to production server
#         env:
#           SERVER_IP: ${{ secrets.SERVER_IP }}
#           SSH_KEY: ${{ secrets.SSH_KEY }}
#         run: |
#           echo "$SSH_KEY" > private_key.pem
#           chmod 600 private_key.pem
#           ssh -o StrictHostKeyChecking=no -i private_key.pem user@$SERVER_IP << 'EOF'
#             docker pull ${{ secrets.DOCKER_USERNAME }}/my-node-app:latest
#             docker stop my-node-app || true
#             docker rm my-node-app || true
#             docker run -d --name my-node-app -p 80:80 ${{ secrets.DOCKER_USERNAME }}/my-node-app:latest
#           EOF
